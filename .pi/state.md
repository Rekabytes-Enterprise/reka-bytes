# State — 2026-09-13 (quiz resilience fix · scenes pushed `e15667d`)

## Current state
- **2026-09-13: QUIZ-PASS RESILIENCE FIX (uncommitted)** — prod run died at GenerateQuiz (missing `title`, bare call, whole job failed, lessons lost). Fix 1: withBamlRetry on both quiz sites (per-module + regen). Fix 2: catch → quiz=null + progress note, class still written. Fix 3: BAML ModuleQuiz.title optional + normalizeQuiz(quiz, moduleTitle) defaults it. Verified: typecheck 7/7, lint, shared 27/27, admin build, format. Not pushed yet.
- **2026-09-13: LEG_* env on prerendered legal/about pages — ANOTHER build-time trap, fixed with force-dynamic (uncommitted)**. User set LEG_OPERATOR_NAME/FOUNDER_NAME/REGISTRATION in Coolify (buildtime+runtime checked) → pages still showed placeholder chips. Cause: about/privacy/terms/cookies are server components with NO dynamic export → statically prerendered during GH Actions build (no LEG_* present) → baked placeholder HTML served from disk forever; runtime env never re-read. `LEG_OPERATOR_NAME`/country looked "working" only because legal.ts has hardcoded defaults ('Reka Bytes'/'Malaysia'). Coolify "Available at Buildtime" checkbox is meaningless for pulled GHCR images. Fix: `export const dynamic = 'force-dynamic'` on the 4 pages → build shows them as `ƒ Dynamic`. Footer left static (only consumes defaulted values). Fourth consecutive build-time-env incident — the rule is in global stacks.md deployment section.
- **2026-09-13: PRD-06 AI-Authored Interactive Scenes SHIPPED (committed `e15667d`, pushed dev)** — all rings R1-R5 done. R1 shared: widget schema +brief/fallbackMarkdown/reviewed + flatten widget section + `src/blocks.test.ts` (27/27). R2 BAML: `WIDGET` enum + raw html/brief/fallback_markdown fields + prompt contract (≤1/lesson, self-contained, 375px, reduced-motion, no reporter authoring) + client regenerated. R2 backend: `services/scene-hardening.ts` (static gate + CSP/meta + reporter injection, idempotent), `toLessonBlocks` widget case (first-valid-wins, hardened inline), `content.service` create+update harden scenes (admin PUT: html-changed → reviewed=false, gate-fail → loud 400), mock fixture gains hardened vibe-loop scene. R3: `scene-frame.tsx` (opaque-origin sandbox iframe + postMessage height + fallback ladder) wired in registry. R4: admin editor ScenesSection (preview/code-view/acknowledge-immediate-PUT + divergence notice `lesson-blocks-notice`). R5: e2e-18 spec + `playwright.scenes.config.ts` override + DESIGN.md §5c. **Verified**: typecheck 7/7 ✅ lint ✅ shared 27/27 ✅ admin+frontend builds ✅ format stable ✅ mock-scene probe (CSP+reporter injected, hostile scenes rejected) ✅. NOT done: e2e-18 run (user's call — needs live servers), real-AI scene smoke, prod tag when deploying (v0.1.5 image rebuild needed).
- **2026-09-12: v0.1.4 cohort feature pushed (`4bca5e2`) + user tagged v0.1.4?** — versions 0.1.4, cohort CRUD/activate/caps live in admin, prod deploy pending user's tag push. Earlier: start.sh migrations shipped (v0.1.2), runtime /api proxy route handler shipped (v0.1.1), prod verified working (login 200, tables exist). Probe account `probe1789196674@test.dev` still in prod applications for the user to reject.
- **2026-09-12: global pi memory updated** — `~/.pi/agent/memory/stacks.md` gained "Deployment — canonical web setup" (proxy/migrations/images/release/Coolify rules, no project attribution) per user: all future projects follow it.
- **2026-09-12: prod admin-login 500 root-caused + FIXED via runtime route handler** — Next rewrites bake destinations at BUILD time; fix = catch-all `app/api/[...path]/route.ts` + `lib/backend-url.ts` in both Next apps, rewrites() deleted; pattern globalized to `~/.pi/agent/memory/stacks.md`.
- **2026-09-12: prod empty-DB P2021 fixed structurally** — backend image CMD = `scripts/start.sh` (migrate deploy on every start → exec tsx).
- **2026-09-12: prod admin-login 500 root-caused + FIXED via runtime route handler (mokara-style, globalized)**. Root cause: Next rewrites destinations evaluated at BUILD time — published images baked `http://localhost:4300`; runtime env can't change it. Fix: catch-all `app/api/[...path]/route.ts` + `lib/backend-url.ts` in both Next apps, rewrites() deleted; 503 unset-in-prod / 502 JSON upstream-fail. **Pattern globalized into `~/.pi/agent/memory/stacks.md` §"Deployment — canonical web setup"** (per user: future projects follow it, no attribution).
- **2026-09-12: prod empty-DB P2021 fixed structurally** — backend image CMD is now `scripts/start.sh`: `prisma migrate deploy` (idempotent) runs on EVERY container start before `exec tsx`; Coolify pre-deploy field never needed again. Verified migration leg locally. v0.1.2 tagged+deployed by user; proxy fix confirmed live in prod (login 401→200, requests reach backend).
- Prod state (user-reported): fresh accounts register+login fine pre-approval (verified via live probe on prod); "Incorrect email or password" they saw = account never persisted (pre-migration registration) or admin-creds-on-student-login (deliberately removed in 2026-08-27 hardening). Probe account `probe1789196674@test.dev` left PENDING in prod for them to reject.
- **2026-09-11: full local CI run GREEN + pushed (`6caf52d`)** — frozen install ✅ · prisma generate ✅ · typecheck 7/7 ✅ · lint ✅ · format:check ✅. `**/next-env.d.ts` in `.prettierignore` (generator output).
- **2026-09-11: full local CI run GREEN + pushed (`6caf52d`)** — exact ci.yml sequence locally: frozen install ✅ · prisma generate ✅ · typecheck 7/7 ✅ · lint ✅ · format:check ✅. Caught a CI-bound landmine: `next-env.d.ts` (Next-owned; dev server rewrote it after the standalone config change) was committed unformatted → format:check would fail on the runner even after the prisma fix. Fix: `**/next-env.d.ts` added to `.prettierignore` (generator output). Pushed; awaiting green CI for `6caf52d`.
- **2026-09-11 (earlier): CI first-run failure FIXED and pushed (`fe69d07`)** — runner failed `pnpm -r typecheck`: `db` TS2307 `./generated/prisma/client` — `db/src/generated` is gitignored, fresh clone has no client (locally it only worked because generate had run previously). Fix: ci.yml generates between install and typecheck (Dockerfiles already did in-image).
- **2026-09-11 (earlier): footer unified to ONE compact design on every public page**.
- **2026-09-11 (earlier): prettier + CI workflow shipped, repo formatted once**. Prettier 3.9 root devDep, `.prettierrc` (singleQuote / printWidth 100 / proseWrap preserve), `.prettierignore`, root scripts `format` + `format:check` (`--ignore-unknown` both). `pnpm format` reformatted **136 files** (mostly line-wrap) — typecheck + lint verified green AFTER. `.github/workflows/ci.yml`: push/PR on `dev` + manual → typecheck · lint · format:check. **No `dev` branch exists yet (only `main`) — CI is dormant until it's created.** Note: working tree now has ~143 uncommitted changes (prettier reformat + docker + workflows) — likely all committed/tagged v0.1.0 together.
- **2026-09-11 (earlier): Docker images + GHCR release workflow SHIPPED & verified**. `docker/{backend,frontend,admin}.Dockerfile` + `.dockerignore` + `.github/workflows/release.yml` (tag `v*`/manual → 3-image matrix → `ghcr.io/rekabytes-enterprise/reka-bytes-*`). Next configs gained `output:'standalone'` + `outputFileTracingRoot` (frontend+admin, no dev impact). All 3 built locally + smoke-run green (backend `/health`, frontend `/` w/ site-nav, admin `/login`). Full gotchas + decisions in AGENTS.md Docker section; Coolify setup doc `docs/development/DEPLOY.md`. NOT yet done: no real tag pushed (user pushes `git tag v0.1.0 && git push origin v0.1.0`), GHCR packages will be private on first push, workflow untested on real runner (only local docker buildx via default arch; CI builds amd64).
- **2026-09-11 (later): sidebar footer redesigned into one proper profile button; logout lives ONLY on /profile**. `SidebarFooter` = single `Link → /profile` (`data-testid="sidebar-profile-button"`, rounded-panel elevated card: LevelRing avatar + name + `view profile` hint + chevron, hover accent). Email + logout REMOVED from sidebar (email only on /profile identity card + fields; logout = only `profile-logout`). `level-ring.tsx` restructured: initials avatar INSIDE the XP ring, `L{n}` chip overlaps bottom-right (`sidebar-level-chip`, ring-2 ring-elevated separates from arc) — testid `sidebar-level-ring` kept; component only used by sidebar. `profile-logout` moved from below-badges to an account-action row right after the identity fields (mono `// end the session on this device` + compact danger pill); e2e-16 only checks `profile-identity` visible — safe. Verified: typecheck ✅ lint ✅ build ✅ dev-server hot compile ✅.
- **2026-09-11: floating site navbar + Log in CTA shipped on all public pages**. New `components/layout/site-nav.tsx` (SERVER component, no 'use client' — static links need no pathname logic): fixed `top-4 sm:top-5` centered pill, `max-w-[1240px]` aligned with page container, `h-14 rounded-full border-line bg-canvas/85 backdrop-blur-md shadow-[var(--shadow-lift)]`; brand = footer's exact reka·bytes wordmark; links `curriculum → /#curriculum` (works from any page) + `about → /about`, hidden <sm (footer nav covers mobile); right = `Log in` primary Button (px-5 py-2.5) wrapped in Link (house pattern). Testids: `site-nav`, `nav-brand`, `nav-login`, `nav-link-{curriculum,about}`. Wired into landing/about/privacy/terms/cookies right after `<main>`; login/register/404 stay chrome-free on purpose (nav would be redundant/noise there; 404 is an immersive scene). Log in button intentionally static — no session check (server component simplicity); revisit if needed. Verified: typecheck ✅ lint ✅ build 13/13 ✅ + curl on live dev server: nav present on all 5 public pages, absent on /login+/register.
- **Dev servers can now be started by agent on explicit ask** (rule updated in ~/.pi/agent/AGENTS.md: background with `nohup … > /tmp/dev-<svc>.log 2>&1 &`, echo log path; unprompted = still forbidden). Currently running: backend :4300 (log /tmp/dev-backend.log), frontend :4301 (log /tmp/dev-frontend.log).
- **2026-08-27 (prior): PRD-05 (Interactive Lesson Kit) DRAFTED, awaiting approval — no code written**.
- **2026-09-09 (latest): 404 scene upgraded to an INTERACTIVE voxel viewer modelled on petalwind — e2e 5/5**. User shared a reference (three.js floating-garden site: click-to-wander / drag-to-orbit / scroll-to-zoom) and asked to record + analyse it, then make ours interactive "not exact same". Installed system ffmpeg 9.0.1 (brew) after discovering Playwright's bundled one is mux-only (no fps filter, can't decode webm→png); recorded the reference + our page with scripted pointer/wheel interactions (`/tmp/record-*.mjs` probes), extracted frames, analysed. Rebuilt `not-found-scene.tsx` with a real orbit camera (rotate world about island centre, auto-fit from projected bounds every camera change so a rotated island never clips), wheel zoom (0.6–2.1×), click-to-wander (ghost cube hops to the clicked tile with arc + olive target ring + 10-petal burst), cursor-velocity wind on petals, plus scene enrichment: stone arch, two benches, flower specks, floating islet, 28 petals, `touch-none cursor-grab` canvas, and a `click to wander · drag to orbit · scroll to zoom` hint pill. **Two renderer bugs found via frame analysis and fixed**: (1) interior side faces caused a brown "waffle" mid-orbit → added `OCCUPIED` set face-culling + `facingCamera()` so all four sides render correctly at any azimuth; (2) depth sort used rz alone → corrected to rx+rz. Also fixed: hint pill/caption swallowed the drag (needed `pointer-events-none` — orbit e2e failed with az unchanged), `lastY` never updated, `staticCanvas` reference dropped, `hop` narrowing inside nested arrow, seam alpha 0.18→0.07. New canvas telemetry `data-view`/`data-hops` lets e2e assert orbit/zoom/wander deterministically; e2e-17 now 5 tests (added interactions test). Verified: frontend typecheck+lint+build ✅ · e2e typecheck ✅ · **e2e-17 5/5 green** vs live servers · mid-orbit/far-orbit/default frames visually confirmed clean. Known minor: one ~20px brown wedge under the front edge at ~290° azimuth (painter's-sort artifact, invisible at default view) — accepted.- **2026-09-09 (latest): custom frontend 404 shipped with isometric voxel scene + e2e-17 video suite — 4/4 green**. `app/not-found.tsx` (server component: metadata WORKS on Next 16, breadcrumb `home / 404`, "Block not found." display h1, mono `// chunk (404, 64, -101)` line, "Return to spawn" CTA, global Footer) + `components/landing/not-found-scene.tsx` ('use client' canvas-2D isometric voxels, ZERO deps: 11×11 grass chunk, 2×2 fountain pool sunk below a 12-stone ring, two sakura trees shedding 18 petals with trails, bobbing debris, dashed olive ghost cube = the missing block; offscreen static layer re-rendered on resize, single rAF for shimmer/bob/petals, deterministic — no Math.random; `prefers-reduced-motion` → one static frame; `data-drawn` marker). First render analysis (screenshot) caught the fountain invisible: 1×1 water fully covered by the ghost cube's oversized ground shadow + tiny stone ring → rebuilt as 2×2 pool (WATER_Y=0.7 below rim y=1), 12-block ring, tree2 moved (8,7)→(8,6) to overhang the pool, shadow re-anchored to water plane. e2e: NEW `playwright.notfound.config.ts` override (NO webServer/globalSetup — main config auto-SPAWNS dev servers when down, forbidden) + `e2e-17-not-found.spec.ts` 4 tests: 4-route sweep (real HTTP 404 status, h1, canvas `data-drawn`, home href, footer, console hygiene filtering the 404's own resource noise), reduced-motion static frame, **animation-alive proof** (two canvas captures 400ms apart must differ — no ffmpeg needed), 390px mobile composition. Run: `npx playwright test --config=playwright.notfound.config.ts tests/e2e-17-not-found.spec.ts`. Found+fixed during e2e: `getByRole('contentinfo')` never matches — footer inside `<main>` loses the landmark role (HTML-AAM). Verified: typecheck (frontend+e2e) ✅ lint ✅ frontend build 13/13 (`/_not-found` prerendered with custom content) · e2e 4/4 green vs live dev servers (user-running). Gotchas flushed to `.pi/memory.md`; override-config rule added to AGENTS.md E2E section.- **2026-08-27 (latest): PRD-05 (Interactive Lesson Kit) DRAFTED, awaiting approval — no code written**. User wants lessons to stop feeling like a blog: learner interactivity + gamified flow, NOT dev/code sims. Plan at `docs/development/PRD-05.md`: 6 interaction block types (`order-steps`, `match-pairs`, `fill-blank`, `classify`, `mini-scenario`, `tap-to-reveal`) as parameterized coded components fed by BAML scenario data; `GenerateOutline` gains `plannedInteractions[]` (≤3/lesson); "model emits truth, code derives shuffles" rule; gradable types inherit sanitize→submit→reveal security contract (tap-to-reveal ships inline); auto-complete extends to attempted interactions; XP reason `INTERACTIVE_SOLVED` +15 first-solve-only via existing @@unique idempotency; BlockEvent `kind` column already exists (no migration). Rings R1 schemas/BAML → R2 renderers → R3 grading/XP wiring → R4 wizard surfaces+fixture. Stepper lesson-flow deferred (candidate PRD-06), dev-tool sims stay in LESSON-PLAN §5.1, sandboxed AI widgets stay Phase C.
- **2026-08-27: Malaysian legal pages shipped (`/privacy`, `/terms`, `/cookies`)** — PDPA-2010-aligned Privacy Policy (12 sections covering all 7 PDP principles + AI Masterclass disclosure + 7/14/21-day access rights), Terms of Use under Contracts Act 1950 + Consumer Protection Act 1999 (14 sections, payment clause written as forward-compatible placeholder since no Stripe yet), and a lean Cookies Policy (only `rb_session` 7-day HTTP-only + 2 localStorage keys; no third-party analytics tracked). Cross-linked from landing footer, login page, and register step-3 (with explicit PDPA Notice & Choice consent sentence before "Submit application"). Placeholders flagged in-page for: legal address, SSM no., preferred privacy-email alias (used `privacy@rekabytes.my` default). Server components with `metadata` exports, blueprint landing aesthetic (paper canvas + mono labels), max-w 860px article column, sticky scroll-margin for any future in-page TOC. Verified: typecheck ✅ · lint ✅ · frontend build 12/12 routes (3 new prerendered static).
- **2026-08-27: Mereka Cipta dropped, operator identity env-driven** — user wants only "Reka Bytes" as the brand/operator; all "Mereka Cipta / mereka cipta" references deleted from source (`app/page.tsx`, `app/{privacy,terms,cookies}/page.tsx`) + README + PRD. Company details migrated to `packages/frontend/.env.local.example` under `LEG_*` prefix (server-only, no `NEXT_PUBLIC_` — Next inlines prefixed vars into client bundle, none of these belong there). Single source of truth = `packages/frontend/src/lib/legal.ts` mirroring mokara's pattern: `OPERATOR {name, registrationNo, entityNote, country, productName}` + `CONTACT {email, postalAddress, hostingRegion, backupRetention}` + `LEGAL_DOCS` registry + `mailtoHref()` helper. Graceful degradation: unset env vars render as `[LEG_*]` placeholder chips rather than blank/invented values (mokara's `<Pending>` pattern, but inline string form for readability). `LEG_OPERATOR_NAME` defaults to `"Reka Bytes"` and `LEG_OPERATOR_COUNTRY` defaults to `"Malaysia"` — setting them is OPTIONAL. All other vars (registration no., email, address, hosting region, backup retention) require explicit config. Footer tagline "mereka cipta × computer science" + copyright line "· mereka cipta" removed; copyright now just `© 2026 reka bytes` and "made in {LEG_OPERATOR_COUNTRY.lowercase}" pulls from env. Landing footer expanded to multi-column (Company / Product / Legal) with `FOOTER_COLUMNS` data array so adding a link is one entry, adding a column is one push. Verified: typecheck ✅ · lint ✅ · build 12/12 routes ✅ · zero "Mereka Cipta" hits in source/docs (except gitignored `.pi/{memory,state}.md` historical context).
- **2026-08-27: real company values loaded into `.env.local`** — pulled from `mokara/packages/frontend/.env`: `LEG_OPERATOR_NAME="Rekabytes Enterprise"`, `LEG_OPERATOR_REGISTRATION="202503277241 (IP0614333-M)"`, `LEG_OPERATOR_ENTITY_NOTE="a business registered in Malaysia under the Registration of Businesses Act 1956, which is not a separate legal person"`, `LEG_OPERATOR_COUNTRY="Malaysia"`, `LEG_CONTACT_EMAIL="hello@rekabytes.com"`. POSTAL_ADDRESS / HOSTING_REGION / BACKUP_RETENTION deliberately left unset per mokara's pattern (pages render the "not published" or chip fallback). Verified by re-build: each value appears in the prerendered `.next/server/app/{privacy,terms,cookies}.html` exactly where the page should show it (privacy+terms get name+reg+email+entity_note; cookies only references email).
- **2026-08-27: SectionHeader numbering retired** — user wants no `§01` / `01 /` page-index pattern anywhere ("we will not use it"). Made `index` optional in `components/landing/section-header.tsx`; when absent the label renders `{name}` alone (NOT omitted — first attempt gated the whole `<p>` on `index`, which silently deleted the WHY / PRIVACY POLICY labels too; caught by post-build HTML grep, fixed to `{index ? `${index} / ${name}` : name}`). Dropped the `index` prop from all 7 usages (3 legal + 4 landing sections); section names kept. Verified: 0 `§0X`/`"0X" /` hits in prerendered HTML, all 7 labels still present.
- **2026-08-27: `/about` shipped with generated founder portrait + media pipeline established** — first real image asset in the project. Generated 3 founder-portrait variants from the user's selfie via pi's `generate_image` (img2img with `reference_image`); user chose the **studio** variant. New `packages/frontend/public/founder/studio-{448,896}.webp` (1341KB PNG → 16KB/61KB WebP, ~22× smaller) encoded with `cwebp` because **sharp/ImageMagick are NOT installed** — so `next/image` production optimisation is unavailable; plain `<img>` + `srcSet` + `fetchPriority` is the pattern here (zero new deps). Portrait is 896×1200 = 0.747 ≈ 3:4, matching the landing's `aspect-[3/4]` instructor block. Added `.pi/media/` to `.gitignore` (scratch renders; only encoded finals ship under `public/`). Page structure: breadcrumb + header, founder (portrait/bio/credentials), why-this-exists, 4 principles, for-you-if / not-for-you-if, 5-step cohort flow, **COMPANY RECORD spec sheet reading `OPERATOR`/`CONTACT` from `lib/legal.ts`** (so legal facts have one source), CTA, Footer. Unknown personal facts render as a loud dashed `<Tbc>` chip rather than invented copy — **only the origin story placeholder remains** now: founder name (`LEG_FOUNDER_NAME="Khairul Anuar Aliasak"`) and GitHub (`LEG_SOCIAL_GITHUB="https://github.com/rekabytes"`) became env-driven via new `FOUNDER` / `SOCIAL` exports in `lib/legal.ts` (unset → chip / renders nothing respectively), LinkedIn dropped at the user's request, and the `<TbcLink>` helper was deleted. Name also feeds the portrait `alt`. Portrait shrunk to `max-w-[17rem]` / `sm:max-w-[19rem]` / `lg:col-span-4 lg:min-h-[23rem]` (was `col-span-5` / `min-h-[32rem]`), text widened to `col-span-7 col-start-6`, `sizes` corrected to the real render widths.
- **2026-08-27: frontend env example is `.env.example` (NOT `.env.local.example`)** — the user renamed it to match the backend/admin convention ("Each package owns its `.env` (examples committed)"). Edit `.env.example` for docs; real values live in `.env.local`. My earlier `.env.local.example` path no longer exists. Verified: typecheck ✅ · lint ✅ · build 13/13 routes (`/about` prerendered static), footer's About link now resolves.
- **2026-08-27: pi media-gen extension bug — REAL cause (my first diagnosis was wrong)** — `generate_image` 500'd on every call. First hypothesis (hardcoded `output: ["image"]` → wrong `modalities`) was WRONG: fixing it to `["image","text"]` turned the 500 into a 404 *"No endpoints found that support the requested output modalities"*. Actual cause: the user's saved default in `~/.pi/agent/media-gen.json` is `bytedance-seed/seedream-5-0-pro`, which is **broken upstream on OpenRouter** — `modalities:["image"]` → 500, `["image","text"]` → 404 (it only advertises image output). No modalities value works; verified by raw curl both ways. Genuine design flaw found alongside it: `CatalogEntry` discarded `architecture.output_modalities`, forcing `buildModel` to guess → fixed properly by carrying per-model modalities through the catalog (populated from OpenRouter, `FALLBACK_IDS` + video entries updated). **`google/gemini-3.1-flash-image` works** (`["image","text"]`, HTTP 200, ~$0.068/image) and is what produced the portraits — passed explicitly via `model=` since the session's loaded code still had the interim hardcode. NOTE: extensions hot-reload with `/reload` (auto-discovered location `~/.pi/agent/extensions/`), no restart needed. User should switch default off Seedream via `/media-model`.
- **2026-08-27: footer globalised, breadcrumb on legal pages** — extracted `components/layout/footer.tsx` (the multi-column brand+links+country/copyright footer) from the landing page; moved `FOOTER_COLUMNS` data array into the same file as the only consumer. Added `components/layout/breadcrumb.tsx` — lowercase mono, `home / <slug>` format with the current item as text (`aria-current="page"`) and parents as links; first render = the legal pages where it replaces the previous `← back to reka.bytes` link. Footer now used on every public page: landing, /privacy, /terms, /cookies, /login, /register. Login + register wrapped in `flex min-h-dvh flex-col` (main = `flex-1`, footer at bottom anchor) so the short pages don't strand the footer mid-screen. Removed inline legal-links block on login + the redundant `← back home` link (footer has both). Removed the bottom cross-link nav (→ Terms / → Cookies / ← Home) from the three legal pages — the footer's Legal column now provides that navigation, and the user explicitly asked to drop it. Verified by post-build HTML grep: footer present on 6/6 public pages, breadcrumb present on 3/3 legal pages, zero "← back to reka.bytes" or "→ Terms/Cookies/Privacy Policy" cross-link navs anywhere. typecheck ✅ · lint ✅ · build 12/12 routes ✅.
- **2026-08-27: ADMIN-session-leak FIXED, verified 15/15 e2e vs live servers** — dedicated `POST /api/admin/login` (`backend/src/routes/admin-auth.routes.ts`, mounted before requireAdmin `adminRoutes` in index.ts); env-admin fallback removed from shared `/api/auth/login`; `useStudentGuard` rejects role=ADMIN (toast + /login, no sessionAtom write); student login page defensively drops ADMIN responses; admin console posts to new endpoint. e2e helpers: new `adminLoginCookie()` (+ private `loginVia`), `approveViaApi(appId)`/`findApplicationIdByEmail(email)` re-credentialled, specs e2e-03 (UI form via console), e2e-11, e2e-13, e2e-15 switched; student flows keep `loginCookie()` on shared login. Verified: pnpm -r typecheck ✅ lint ✅ (fixed unused 'user' var in admin login page) · frontend+admin builds ✅ · live API probes: admin endpoint 200+rb_session, admin creds on student endpoint now uniform 401 · e2e 15/15 green vs live servers (auth suites 03/07+11+12+15 = 9/9 in 14.3s; student flows 09×3+10+14+16 = 6/6 in 12.9s). e2e-13 still needs AI_MOCK=1 backend (environmental). Full root-cause chain + "never re-add privileged shortcuts to shared login endpoints" logged in AGENTS.md Decisions.
- **2026-08-27 (latest): ADMIN-session-leak diagnosed, NOT yet fixed** — user reports admin creds sometimes log their student frontend in "as admin". Root cause = 3 compounding gaps (full traces verified, DB probed): (A) `POST /api/auth/login` has a GLOBAL env-admin fallback (`matchesEnvAdmin` checked before DB lookup) reachable from ANY form incl. student login; (B) ONE shared `rb_session` cookie per hostname — both apps proxy `/api/*` via Next rewrites and cookies are NOT port-scoped, so :4301/:4302 share a jar entry and last-login wins (stale admin session auto-authenticates the student UI); (C) `useStudentGuard` checks only `status==='APPROVED'` while `envAdminUser()` returns APPROVED+ADMIN → full student shell renders under admin identity, progress writes 403 (`requireRealStudent`) = the old "progress not tracking" incident. DB probe: ADMIN_EMAIL collides with NO User row; all 42 users role=USER (no privilege escalation — `resolveSessionUser` re-reads DB per request; `/api/admin/*` guarded by requireAdmin). Proposed fixes awaiting approval: (1) dedicated `/api/admin/login` for env-admin, remove fallback from shared login (e2e helpers + e2e-03/11/13/15 need pointing at it), (2) guard rejects role=ADMIN, (3) local-dev cookie isolation via browsing admin on `127.0.0.1:4302` vs frontend `localhost:4301` (zero-code), optional (4) drop admin lesson-preview reads on /api/learn/*.
- **Frontend light theme shipped same day (color-only)** — user-requested palette flip of `packages/frontend` with no functional/toggle changes. Approved "Soft Terminal: Paper" hex set landed in `packages/frontend/src/app/globals.css` `@theme` (`canvas #FAFAF7`, `elevated #FFFFFF`, `inset #F0F1EC`, `ink #14161A`, `muted #565E66`, `faint #9AA1A9`, `accent #4E7700`, `accent-hover #3E6200`, `accent-dim #558006`, `accent-ink #FFFFFF`, `success #15803D`, `warning #A16207`, `danger #DC2626`, `info #0369A1`, `line #E3E5DE`, `line-strong #C6CAC0`). Recipes flipped: `color-scheme: light`, paper shadows (`rgb(20 22 26 / 0.08/0.14)` instead of black ambient 0.35/0.45), `.blueprint-grid` lines `rgb(20 22 26 / 0.05)`, `.glow-accent` wash `rgb(198 255 74 / 0.16)`. Hardcoded dark spots fixed: `byte-stream.tsx` (clearColor → #FAFAF7, mid-grey mono bits + olive accent), `instructor-flow.tsx` (local light palette literal — does NOT touch shared `colors` because admin also uses it), `lib/celebrations.tsx` (fallback hex + ink for the 3rd confetti piece so it reads on paper). Verified: typecheck ✅ · lint ✅ · frontend build 9/9 routes ✅ · all text contrast WCAG AA. No functional changes — `useTheme`/`themeModeAtom` untouched. Shared package + admin still dark. Decision logged in AGENTS.md.
- **PRD-04 shipped 2026-08-25** (Soft Terminal reskin + XP ledger + game UI, 22/22 e2e).
- **2026-08-27 (latest): outline-approval buttons locked-bug fixed** — after the BAML job expired, the user navigated back to step 3 and all three buttons were stuck at 40% opacity with no escape. Root cause: `approvingAtom`/`regeneratingAtom` are module-scoped Jotai singletons that survive React Router navigations; the `disabled={approving||regenerating}` prop had no recovery path. Fixed in `packages/admin/src/app/(console)/ai-masterclass/step/3/page.tsx`: (a) reset both atoms in the mount effect; (b) derived `canInteract = !approving && !regenerating && job?.status === 'awaiting_approval'` for the approve/regenerate buttons; (c) "start over" stays clickable whenever nothing is in flight (escape hatch). typecheck + lint clean. Rule logged in AGENTS.md under Jotai conventions.
- **2026-08-27: BAML timeout → unlimited** — "Lost contact with the job" error on WriteLesson was caused by BAML silently ignoring flat timeout keys; no `http {}` block defaulted to 300s, too short for reasoning-model WriteLesson (~445s observed). Fixed by adding `http { request_timeout_ms: 0 }` to the `OpenRouter` client in `packages/baml/baml_src/curriculum.baml`. Regenerated + typecheck + lint all clean. See AGENTS.md for the updated gotcha entry (no env-var override works — only the `http {}` block is read).
- **PRD-04 shipped 2026-08-25** (Soft Terminal reskin + XP ledger + game UI, 22/22 e2e).
- **2026-08-26 (latest v2): idle animations amplified** — user couldn't see the first pass; amplitudes ~2x + sheen sweep added, stagger bug fixed (shorthand resets animation-delay — see AGENTS.md). Proof frames idle-frame-{a,b,c}.png.
- **2026-08-26 (latest): badge idle animations** — unlocked-only subtle motion (sway/rock/flicker/twinkle/pulse/glow/bob), CSS in globals.css, staggered per tile. Verified: typecheck/lint/build ✅, e2e-16 ✅, probe asserted `rb-badge-sway` applied + transform moves between samples + locked badges `animation: none` (temp spec deleted; artifact `packages/e2e/artifacts/badge-idle-probe.png`). Full-color pass earlier same day (see AGENTS.md).
- **2026-08-26 (later): badge full-color gamified pass** — per-badge hues + gradients (see AGENTS.md decision). Verified: typecheck/lint/build ✅, e2e-16 ✅, visual probe screenshots at `packages/e2e/artifacts/badge-colors-{probe,profile}.png` (7/10 badges unlocked in probe via temp spec, now deleted). Streak badges + comeback-kid stayed locked in probe (need history).
- **2026-08-26: `/learn` redesigned** — killed the redundancy (ModuleMap + accordion both listed all lessons). Now one `components/student/class-path.tsx`: per-class head (animated progress bar, `continue →` CTA / `all complete` chip) → module cards on a vertical rail (`01`-numbered nodes, complete/current/up-next badges) → lessons as always-visible pill rows (current = accent tint + `resume` chip) → module-quiz dashed tail rows (previously unreachable from /learn). `module-map.tsx` deleted; `/learn/[lessonId]` viewer untouched. Old e2e screenshot artifacts in `packages/e2e/artifacts/` are stale.
- **2026-08-26: custom badge SVGs** — `badge-art.tsx` "Chip Emblems" series replaces the ★/○ glyphs (see AGENTS.md decision). Verified visually locked+unlocked via playwright probe; probe artifacts kept at `packages/e2e/artifacts/badges-{locked,unlocked}.png`, `profile-full.png`. Polish pass: centered lock-overlay on locked chips (canvas knockout plate + lock glyph — glyph edges stay visible as teaser), removed tile `opacity-70` double-dim, reserved 2-line label height so hints align, gem sparkle + stronger facets, bigger halfway flag, hover borders on tiles.
- Verified this session: pnpm -r typecheck ✅ · pnpm -r lint ✅ (fixed pre-existing unused `LevelInfo` import in `shared/src/game/badges.ts`) · shared unit tests 22/22 ✅ · frontend build ✅ · e2e-09/10/11/12/14/16 green against live dev servers.

## Next steps
1. **Prod deploy**: user needs `git tag v0.1.5 && git push origin v0.1.5` when ready (scenes + quiz resilience + force-dynamic legal pages in the image; no DB migration).
2. **Phase B** — simulation template library (`docs/development/LESSON-PLAN.md` §11).
3. One real-AI WriteLesson smoke (`packages/backend/scripts/ai-smoke.ts`) to validate scenes + v3 schema on the live model.
4. e2e-18 scenes spec run (override config, needs live servers) + re-run cohorts e2e.

## Key decisions this session
- Light theme = **color-only, frontend-scoped, no toggle/wiring**. Single palette applied directly to `@theme` tokens + recipe colors (shadows/grid/glow/color-scheme) + the three hardcoded dark spots in frontend code. Shared `colors` constant left dark so admin is unaffected — `instructor-flow.tsx` got a small local palette literal instead of importing shared. `useTheme` hook left as-is (mode atom is unused; `isDark: true` hardcoded).
- Frontend builds, renders, badge SVGs (full-color BADGE_COLORS) all stay readable on the new paper canvas — locked badges' "statue" grayscale + tinted tile glow still work because their fill colors are literal hexes independent of the `@theme` accent.
- Redundancy fix = **one presentation, not two**: collapsible dropdown dropped in favor of always-visible path (small lesson counts; e2e clicks `lesson-link-*` without toggling → more robust).
- `module-map`/`map-dot-*` testids kept frozen even though markup changed (DESIGN §5a rule).
- ClassPath owns the whole per-class section (head + path) so `e2e-14`'s `section:has(module-map)` locator keeps working.

## Blocked / waiting on
Nothing.
