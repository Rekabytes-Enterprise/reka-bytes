# State — 2026-09-18 (Whimsery posts merged into one field-report post)

## Current state
- **2026-09-18 (latest): 3D DESK SWAPPED IN — model-viewer replaces canvas scene** (user disliked the canvas voxel desk). User supplied `public/assets/3D/Table-Laptop.glb` (5.0 MB floating-desk glb). Added `@google/model-viewer` dep + `types/model-viewer.d.ts` (React 19 JSX augmentation — NO `as any`, attributes enumerated). New `journal-desk-model.tsx`: dynamic-imports the web component on mount (never in main chunk), renders only once registered (skeleton state), auto-rotate gated by reduced-motion, `disable-zoom` + `touch-action pan-y` so the page scroll never gets hijacked, lazy loading, testid `journal-desk-model`. `journal-desk.tsx` uses it (caption "drag to orbit"); canvas scene file DELETED (uncommitted, was disliked). Rodin reference-based gens: 2 completed (High tier) in Hyper3D workspace — user chose their own glb instead. Verified: typecheck ✅ lint ✅ build 10/10 ✅ format ✅. NOT committed yet: glb asset, model-viewer dep + lockfile, journal-desk files, page.tsx swap, social.md pilot, memory/state.
- **2026-09-18: 3D MODEL AS JOURNAL COVER** — `we-shipped-our-own-journal-then-used-it` gained `cover.jpg`: a real render baked from the glb via headless Chrome-for-Testing + @google/model-viewer UMD bundle (swiftshader-webgl, virtual-time-budget 25s; gotchas: hide the progress bar with `--progress-bar-height: 0px`, narrow `field-of-view` for a tight hero crop, `--allow-file-access-from-files` needed for file:// glb fetch). cover.jpg 1280×720, 49 KB. Frontmatter updated; probe: cover rewrites + asset resolves ✅, 6 admin rows no warnings. Cover bake recipe reusable for future posts. NOT committed yet. — "From the desk" section** (user approved: Tier 2 centerpiece + Tier 1 polish, NO navbar change). New `journal-desk.tsx` (client section) + `journal-desk-scene.tsx` (canvas 2D isometric desk: orbit/zoom, click desk → top journal hops there w/ paper burst, click stack → returns home, wind pushes loose sheets; reduced-motion static + rerenderNow; telemetry data-drawn/data-view/data-hops; face-culling restored after lint caught it — the waffle gotcha bit again). Tilt cards via inline useTilt (pointer → perspective transform, reduced-motion gated). PeekDialog modal (framer-motion, Escape/backdrop, testids journal-peek*). Featured slot testid `home-featured-journal` FROZEN — e2e-19 still asserts it + 'free consultation' text. Replaced `<FeaturedJournalPost />` with `<JournalDesk />` in page.tsx (old component kept, now unused — delete later if wanted). Rodin Gen-2.5-High desk model generated (user asked for high quality; a Medium variant also completed — both in Hyper3D workspace, glb files available, display URLs reported). NOT viewed live (servers down, user's call). Verified: frontend typecheck ✅ lint ✅ build 10/10 ✅ format:check ✅. NOT committed yet.
- **2026-09-18: `journal-social` skill created** — global at `~/.agents/skills/journal-social/`. Every journal post now ships with a companion Manglish Threads/X thread at `content/journal/{date}-{slug}/social.md` (inside the asset folder — engine-invisible, ships with the post's commit). Voice: Manglish BM+EN, zero jargon, what-we-learned-and-how-we-fixed, standalone (no journal link by default), max 7 posts, ≤~500 chars/post with real char counts in headings. `journal-write` wired: step 2 loads journal-social, step 5 writes both files, verify/report cover both. Pilot written: `2026-09-18-green-isnt-proof/social.md` (6 posts, 157–251 chars each, char counts verified via node counter). Verified: format:check ✅ · engine probe: 6 admin rows, no warnings, social.md invisible to post scanner ✅. NOT committed yet.
- **2026-09-18: JOURNAL SKILLS GLOBALIZED** — three skills moved from `.pi/skills/` to `~/.agents/skills/journal-{write,sensitivity,images}` (single copy, no drift; global skills load in reka-bytes sessions too). Rewrites: anchor path baked in (`/Volumes/Dev/users/Dev/Desktop/Github/reka-bytes`), cross-project publishing rules (subject git untouched, reka-bytes voice/CTAs, verification from anchor root), sensitivity gained the subject-project NEVER rule (test runs twice). Project-local SKILL.md files deleted — PUSHED as `43b8fe4`.
- **2026-09-18 (later): NEW POST `green-isnt-proof` written** — user-approved draft → file. Marketing piece on the studio's UAT practice (NOT Whimsery-specific): couldn't afford a QA tester → built own UAT tooling (AI films every run, debugs with screenshot evidence). Model name HIDDEN per user call ("a fast AI model" — glm-5.3-flash stays private). Featured: no. Tags studio/quality/ai-development. Cover = hand-authored SVG (image API 402 again — credits still out) in `2026-09-18-green-isnt-proof/cover.svg`, verified via Chrome-for-Testing headless render (chromium-1234 binary works for SVG inspection). Probe: parses, 4 min read, no leaks, no code fences, admin rows 6 no warnings. PUSHED as `3d73eb5`.
- **2026-09-18: SIX WHIMSERY GAME POSTS MERGED into ONE** — user asked ("too many whimsery journal"). Deleted 6 untracked posts + folders (`eight-bugs`, `five-lessons`, `cathedral`, `button`, `no-account`, `server-events`), created `2026-09-18-seven-days-building-whimsery.md` — "Seven days building Whimsery — the field report".
  - Sections in player order: splash/cathedral → login wall → AUTO button → combat juice → five sprint rules → eight-bugs table. Cross-duplicated lessons deduped (materials/update-loop/anti-enumeration/first-sighting each appear once, cross-referenced). ONE CTA at the end (was 6 identical).
  - Images: all 6 original covers reused as inline section images in `2026-09-18-seven-days-building-whimsery/` (`login-wall.jpg`, `auto-button.jpg`, `combat-juice.jpg`, `two-wizards.jpg`, `first-quest.jpg`) + cathedral recompressed q55 → `cover.jpg` 155 KB. Real screenshots, no new generation needed.
  - Game is named **Whimsery** (Fields of Prontera / HIGH WIZARD) — visible on covers; user's own word.
- **Verification**: shared tests 41/41 ✅ · `format:check` ✅ (also reformatted `we-shipped-our-own-journal-then-used-it.md` — was unformatted) · tsx service probe ✅ (list total 4, merged post parses, read 14 min, all 6 asset paths rewritten to `/api/public/journal-assets/2026-09-18-seven-days-building-whimsery/…`, old slugs 404, traversal blocked, admin rows 5 warnings none).
- Published journal is now 4 posts: free-prd (featured), seven-days-building-whimsery, we-shipped-our-own-journal, your-app-idea (+1 draft behind-the-classroom-engine). e2e-19/20/21 frozen slugs untouched.

## Next steps
1. User's call: commit + push `green-isnt-proof` (written this session, uncommitted).
2. Live-server smoke + e2e-19/20/21 runs (still pending from 2026-09-16, user's call).
3. Tag v0.1.6 → GHCR images with journal content.
4. Phase B (simulation template library, LESSON-PLAN §11) + real-AI WriteLesson smoke — queued.
5. Main-page content backlog (portfolio/showcase, FAQ, WhatsApp CTA) — unchanged.

## Blocked / waiting on
User call: green-isnt-proof commit, e2e runs, deploy tag.
