# Memory — Reka Bytes (distilled facts & gotchas)

## Site split: company `/` vs academy `/academy` (2026-09-15, pushed dev)
- **Route map**: `/` = COMPANY page (Reka Bytes app-dev studio — mobile apps / web apps / AI products cards; flexible-budget engagement models fixed-scope · MVP-first · retainer; **START SMALL pricing**: FREE consultation, FREE concept mockup, **full PRD from RM 150** in PDF/MD framed as an AI-reference for phase-by-phase dev incl. FR, NFR, error-handling spec, e2e test plan for UAT); `/academy` = the old landing (hero, why, curriculum anchor `#curriculum`, instructor, join CTA + seats meter, gained metadata export); `/academy/about` = old `/about` (breadcrumb home → /academy). `/privacy` `/terms` `/cookies` stay at ROOT for URL stability. Login/register/student routes untouched.
- **Nav + footer split**: `SiteNav` + `Footer` both take `variant?: 'main' | 'academy'` — **default 'main'**. main = brand(→/) + `Academy →` button (`nav-academy`), **no Log in**; academy = brand + curriculum/about links (`/academy#curriculum`, `/academy/about`) + Log in (`nav-login`). Login is only reachable from academy side (user requirement). Legal pages + 404 use defaults; login/register/status/about/academy pass `variant="academy"` (footer academy = old blurb + apply-to-cohort; footer main = studio blurb). Footer Company column: About → /academy/about; Product column gained Academy → /academy.
- All prior landing sections/components reused as-is under academy; hero `#curriculum` anchor is same-page. e2e-01 updated `goto('/') → goto('/academy')`, NOT re-run (user's call). `join-register-cta`, `seats-chip` testids unchanged.

## Leads funnel (2026-09-15, pushed dev)
- **Public form → backend → admin triage pipeline for the company-site "start small" offers.** Shared `schemas/leads.ts`: `leadCreateSchema` (name/email/phone?/service/platform/budget/message 20–4000 + `website` honeypot field, enums defaulted), `leadUpdateSchema` (status + adminNotes), `LeadDTO`, consts `LEAD_{SERVICES,PLATFORMS,BUDGETS,STATUSES}`. DB `Lead` model + 4 enums (`LeadService` CONSULTATION/MOCKUP/PRD/BUILD · `LeadPlatform` MOBILE/WEB/BOTH/UNSURE · `LeadBudget` UNDER_5K/RANGE_5_15K/RANGE_15_50K/OVER_50K/UNSURE · `LeadStatus` NEW/CONTACTED/WON/LOST/ARCHIVED), migration `20260914064039_leads`, applied + client regenerated. No user relation — submitters aren't users.
- Backend: `services/lead.service.ts` (createLead/listLeads/updateLead, AppError.notFound on missing id) · public `POST /api/public/leads` `rateLimit('leads')` + zValidator; **honeypot (`website` filled) returns fake 201 `{id:'ignored'}` without DB write**; admin `GET /api/admin/leads` + `PATCH /api/admin/leads/:id` (inside requireAdmin `adminRoutes`).
- Admin: sidebar `Leads` (Handshake icon, between Applications and Cohorts) + single-page triage at `/leads`: status filter tabs, expandable rows (`lead-row-<email>` → `lead-detail-<id>`), status move buttons (`lead-status-<lower>`), private notes textarea + save (PATCH adminNotes, null on empty). No separate detail page (unlike applications) — deliberate MVP choice.
- Frontend: `components/landing/lead-form.tsx` ('use client') on `/` section `id="start"` replacing the old mailto CTA block. Native sr-only radios + state-styled chips (do NOT use peer-checked for visibility — labels are state-controlled), client safeParse → `apiFetch POST`, field errors inline + toast, success panel (`lead-success`) with "send another". Hero CTA now `#start` anchor; mailto (home-mailto-cta) + academy (home-academy-cta) kept as alt links in left column. `home-bottom-cta` testid REMOVED (replaced by `lead-submit`). PDPA consent line under submit. Option label maps are `Record<Enum, string>` — compiler-exhaustive when shared enums change.
- Verified: `pnpm -r typecheck` 7/7 ✅ lint ✅ frontend build 16 routes ✅ admin build (18, /leads ○) ✅ format stable ✅ Lead table confirmed in dev DB. NOT run: live API probe/e2e (servers down — user's call).

## Quiz-pass resilience (2026-09-13, prod incident fix)
- **A quiz that stays unparseable must never kill a generation.** Prod: GenerateQuiz returned questions without `title` → BamlValidationError → bare call (NO withBamlRetry — the one pass that had missed the contract) → whole resume failed → 15-40 min of written lessons LOST (single transactional write happens after ALL passes). Fix: (1) GenerateQuiz wrapped in withBamlRetry (both pass sites: per-module + per-quiz regen), (2) catch → `quiz: null` + progress note "Quiz failed for {module} — module written without one" (write path + /learn already handle quiz-less modules), (3) BAML `ModuleQuiz.title` now OPTIONAL (prompt says platform supplies it) + `normalizeQuiz(quiz, moduleTitle)` defaults `quiz.title ?? moduleTitle`. Never let a cheap pass destroy an expensive one.

## Scenes / widgets (PRD-06, shipped 2026-09-13 — uncommitted)
- **AI can now emit ONE interactive HTML scene per lesson** (`widget` block, BAML `WIDGET`): complete self-contained doc, mandatory `brief` + `fallbackMarkdown`, `reviewed:false` server-forced. Pipeline: `hardenSceneHtml` (`services/scene-hardening.ts`) = static gate (external URLs/network APIs/forms/nested iframes/window.top/storage/cookies → block DROPPED, lesson degrades) + injects CSP `<meta>` (`default-src 'none'; script-src 'unsafe-inline'; ...data: URIs only`) + height-reporter `<script data-rb-scene="bridge">` into stored HTML. **Idempotent via `data-rb-scene="bridge"` marker.**
- Render: `scene-frame.tsx` `<iframe sandbox="allow-scripts">` (NO allow-same-origin — opaque origin), srcDoc, height via postMessage (only `{source:'rb-scene',type:'height'}` from `event.source === iframe.contentWindow`, clamped 240–4000, 8s no-report → fallback). Fallback ladder: unreviewed → `scene-review-pending` (+fallbackMarkdown) · timeout/oversize → `scene-fallback`. `sanitizeBlocksForStudent` passes widget whole (student needs `reviewed`).
- Review gate: student NEVER gets an unreviewed iframe. Admin editor `ScenesSection`: preview (fixed h-80 sandboxed), read-only code view + apply-html-edit (resets review), **acknowledge = immediate blocks-only PUT** (`PUT /api/admin/lessons/:id { blocks }`) — only enabled with no unapplied html draft. Backend `updateLesson`: widget html changed vs stored → `reviewed=false` + re-harden; unchanged → keep flag; gate-failing admin scene → loud 400 (not silent drop). `createLesson` hardens too. Mock path (`mock-output.ts`) bypasses toLessonBlocks → **hardens its own scene via `hardenSceneBlocks`** (vibe-coding-loop widget in lesson 1).
- e2e-18 written, NOT run (user must run): `playwright.scenes.config.ts` override (no webServer) — unreviewed gate, sandbox attr + height-sync + opaque-origin `top.document` throws, no-reporter timeout fallback, admin ack → student sees frame. Scene HTML in spec ships its own reporter (fixture bypasses server injection).
- Prettier markdown trap: raw `<style>`/`<script>` tokens in md prose make prettier treat following lines as an HTML block (trailing whitespace survives --write forever) — backtick them.

## Cohorts (dynamic capacity, shipped 2026-09-12)
- **Capacity is per-cohort in the DB** (`Cohort.cap`); `COHORT_CAP` constant is GONE. Migration `20260912080204_cohorts`: CREATE `Cohort` + `Application.cohortId` (nullable, FK SET NULL, indexed) + seed "Cohort 001" cap 5 isCurrent + backfill ALL applications into it. Applications are STAMPED at registration; cap checks (register + approve) count APPROVED users through `application.cohortId` — scoped to the application's own cohort, so pending 001 apps stay reviewable against 001's cap after 002 activates.
- Exactly one `isCurrent` cohort = where new registrations land + what `/api/public/seats` reports (seats DTO gained `cohort: name`; activation swap = one transaction: updateMany false → update true).
- Admin control: `/api/admin/cohorts` (GET list, POST create, PATCH :id name/cap, POST :id/activate) + `/cohorts` console page (InlineText for name/cap, activate button, approved/pending/total counts; nav icon Layers). Approve dialog + applications rows show the cohort name.
- `SessionUser.cohortName?` — USER role only, fetched from application.cohort in login + /me (env-admin: absent). Powers dynamic copy: status "welcome to {cohort}", dashboard banner + `cohort · live` chip, seats-meter chip/block.
- e2e `fillApprovedSeats` helper was UPDATED: seat-cap counting goes through Application rows now, so fillers need `Application` inserts stamped with the current cohort (E2E-06 keeps working). **E2E suites not yet re-run — user's call.**
- Decision: cap counts APPROVED only (not pending) — a full cohort + pending queue = pendings stuck until admin raises the cap; counts are visible on /cohorts. Marketing copy (landing hero, about "five seats", layout meta) intentionally stays static hand-edited copy.

## Frontend 404 (`app/not-found.tsx` + `components/landing/not-found-scene.tsx`)
- Custom "Block not found" 404, **now an interactive voxel viewer** modelled on petalwind (three.js reference site): drag to orbit, wheel to zoom, click a tile and the ghost block hops there in a petal burst; cursor velocity becomes wind on the petals. Scene = 11×11 grass chunk, 2×2 fountain pool sunk below a 12-block stone rim, stone arch across the back edge, two benches, two sakura trees, flower specks, a floating islet, 28 falling petals. **Canvas 2D, zero dependencies** — no three.js; house pattern is canvas components in `components/landing/`.
- **`metadata` export WORKS in `not-found.tsx` on Next 16** — `<title>Block not found — Reka Bytes</title>` confirmed in prerendered HTML (old Next lore says it's ignored; it isn't here).
- Renders inside root layout, OUTSIDE the `(student)` group — no sidebar. `notFound()` from nested routes (bad `/learn/[lessonId]`) lands here too.
- Testids: `error-404`, `error-404-canvas`, `error-404-home`. Canvas telemetry read by e2e: `data-drawn="1"` after first paint (both animated + reduced-motion paths), `data-view="<azDeg>|<zoom>"` and `data-hops="<n>"` updated per frame — this is how orbit/zoom/wander get asserted deterministically instead of pixel-diffing.

## Isometric voxel renderer gotchas (all cost a debugging cycle)
- **Face culling is mandatory once the camera rotates.** Drawing all 3 faces per block looks fine at the default azimuth but turns a flat platform into a brown "waffle" mid-orbit (interior side faces poke over neighbours' tops). Fix: `OCCUPIED` set + draw a side face only when the neighbouring cell is empty AND the face is camera-facing (`facingCamera(nx,nz)` = rotated normal's rx+rz > 0). Also lets all four sides work at any angle instead of inside-out geometry.
- **Depth sort must use `rx + rz`** (both rotated components), not rz alone — sorting on rz mis-orders blocks sharing a row. Ties break on `y`.
- Terrain re-renders into an offscreen canvas only when the camera key (`az|zoom`) changes; idle frames blit + draw ~40 dynamic paths. `computeView` projects all block corners each camera change so a rotated island can never clip the band.
- **Any overlay positioned over an interactive canvas needs `pointer-events-none`** — the hint pill sat exactly where the e2e drag started and silently swallowed the pointerdown (orbit test failed with az unchanged). Same for the fig caption.
- Determinism: index math, no `Math.random` (SSR/screenshots/videos reproducible). Pool cells ARE valid wander targets (the ghost floats over water anyway) — keeps click targets deterministic for tests.
- **`<footer>` inside `<main>` has NO implicit `contentinfo` role** (HTML-AAM) — `getByRole('contentinfo')` finds nothing on every public page; use `page.locator('footer')`.
- **Hoisted `function` declarations lose TS null-narrowing of captured `const`s.** Inside a `useEffect` after `if (!ctx) return`, nested `function`s referencing `ctx` still error TS18047. Fix: const arrows after the guard. Also true for reassignable `let hop` — capture `const h = hop` before using it inside a nested arrow.

## E2E
- e2e-17 (`tests/e2e-17-not-found.spec.ts`, 5 tests): 4-route sweep (real HTTP 404, painted canvas, chrome, console hygiene), reduced-motion static frame, animation-alive (two canvas captures 400ms apart differ), 390px mobile composition, and **interaction** (drag→azimuth moves >15°, wheel→zoom increases, click→`data-hops` increments). Run via the override config only.
- **`playwright.notfound.config.ts`** — no `webServer`/`globalSetup`, so it never spawns dev servers. The MAIN `playwright.config.ts` auto-starts backend+frontend+admin when they're down.
- **ffmpeg**: system-wide 9.0.1 installed via brew (2026-09-09). Playwright's bundled `~/Library/Caches/ms-playwright/ffmpeg-1011` is **mux-only** — no `fps` filter, can't decode webm→png, useless for frame extraction. For analysis: `ffmpeg -i in.webm -vf fps=1.5 out-%03d.png`, and `crop=1280:400:0:0` to isolate the scene band from full-page shots. Periodic PNG screenshots during a scripted interaction beat video frames for still analysis (uncompressed).
- Playwright's own `error-context.md` (page YAML snapshot) — read it before theorising; it showed the page was perfect and only my locator was wrong.
