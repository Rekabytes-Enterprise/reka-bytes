# PostgreSQL — matches docker-compose.yml defaults
DATABASE_URL="postgresql://reka:reka_dev_password@localhost:5433/rekabytes?schema=public"

# Admin credentials — admin login validates against these directly (no DB row).
# Set them in packages/backend/.env; the backend is the single source of truth.